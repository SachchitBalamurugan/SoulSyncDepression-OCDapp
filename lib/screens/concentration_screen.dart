import 'package:flutter/material.dart';

class ConcentrationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Concentration Video'),
      ),
      body: Center(
        child: Text('Details about Concentration Video'),
      ),
    );
  }
}
